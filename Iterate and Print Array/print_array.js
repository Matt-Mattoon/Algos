// Iterate through a given array, printing each value.
function print_array(arr){
    console.log('print_array');
    for(i=0;i<arr.length;i++){
        console.log(i, arr[i]);
    }
    return
}

print_array(['Mert','was','here.']);